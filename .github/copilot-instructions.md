Always look for a venv folder in the root of the repository, and if it exists, activate it before running any code. 

Make it so that the code will error instead when run and will not generate dummy data UNLESS a flag is passed in to generate dummy data.

Let the user know if a file is too big to be run. If it is, try using `head` to get the first 100 lines.